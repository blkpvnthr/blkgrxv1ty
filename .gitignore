# dependencies
node_modules/

# build output
dist/

# local env (Firebase values are inlined into the client bundle at build time,
# but keep the source .env out of version control as hygiene)
.env
.env.*
!.env.example

# firebase
.firebase/

# misc
.DS_Store
*.log
